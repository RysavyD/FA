@model IEnumerable<_3F.Web.Models.Discussion.DiscussionMainPageModel>

<div class="box">
    <div class="box-header blue-background">
        <div class="title">
            <div class="icon-comment-alt"></div>
            O čem diskutujeme
        </div>
        <div class="actions">
            <a class="btn box-collapse btn-xs btn-link" href="#">
                <i></i>
            </a>
        </div>
        <div class="clearfix"></div>
    </div>
    <div class="box-content" style="background-color: #f4f4f4">
        <div class="box-content box-statistic">
            @{
                //var links = Model.Select(d =>
                //    string.Format("<a href='{0}'>{1}</a>"
                //        , Url.Content("~/Diskuze/Detail/" + d.HtmlName)
                //        , d.Name));
                var links = Model.Select(d =>
                    string.Format("<a href='{0}'>{1}</a>"
                        , d.HtmlName
                        , d.Name));
            }
            @Html.Raw(string.Join("<hr/>", links))
        </div>
    </div>
 </div>